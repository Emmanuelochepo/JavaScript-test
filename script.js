// Addition with Function
function add7(num) {
  alert(num + 7);
}
// add7(10);

// Multiplication With Function
function multiplyNo(num1, num2) {
  alert(num1 * num2);
}
// multiplyNo(3, 2);

// To Uppercase with Function.
function toUppercase() {
  let text = "emmanuel";
  return text.toUpperCase();
}
// alert(toUppercase());

// Extract last letter from string using a function
function lastLetter() {
  let letter = "Emmanuel";
  return letter.slice(-1);
}
alert(lastLetter());
